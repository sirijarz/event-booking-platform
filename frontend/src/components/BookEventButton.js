import React from "react";

const BookEventButton = ({ eventId }) => {
  // Handle event booking logic here
  return <button className="BookEventButton">Book Event</button>;
};

export default BookEventButton;
